---
# handlers file for ElasticSearch & Kibana
- name: "ElasticSearch & Kibana reload"
  listen: "EFK reload"
  systemd:
    name: "{{ item }}"
    state: "restarted"
    enabled: True
  loop:
    - "elasticsearch"
    - "kibana"